import { test, expect } from '@playwright/test';

/**
 * NOTE: このファイルは意図的に空のままにしています。
 *
 * コーディングガイドラインによると、E2Eテストは test/e2e/ に配置する必要があります。
 * このファイルはPlaywright Agentによって自動生成されるため、削除すると再度作成されてしまいます。
 *
 * 実際のE2Eテストは以下に配置されています：
 * - test/e2e/tests/seed.spec.ts - 管理画面の基本的な環境確認テスト
 * - test/e2e/tests/verify-*.spec.ts - ダミーサーバーと環境設定の検証テスト
 * - test/e2e/tests/admin/ - 管理画面のテスト
 * - test/e2e/tests/client/ - クライアント画面のテスト
 */

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
    // このテストは意図的に空です。
    // 実際のテストは test/e2e/ に配置されています。
  });
});
